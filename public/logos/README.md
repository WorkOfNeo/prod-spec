# Output Builder logos

- `contrast.svg` (preferred) or `contrast.png` / `contrast.jpg` — the
  Contrast logo, rendered by the `{{logo:contrast}}` variable. Drop the
  file here and commit it; no code change needed.
- The CUSTOM logo is uploaded in the app (Output builder → Logos card)
  and stored in the database — it does not live in this folder.
