// The doc type every carton marking carries, on OutputLayout.docType and on the
// JobAsset the runner produces from it. Kept in its own module so the board, the
// group library and the tests share one spelling of the string.
export const CARTON_MARKING_DOC_TYPE = "CARTON_MARKING";
