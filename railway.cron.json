{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'cron service — skipping app build, runs scripts/cron-tick.mjs'"
  },
  "deploy": {
    "startCommand": "node scripts/cron-tick.mjs",
    "restartPolicyType": "NEVER"
  }
}
